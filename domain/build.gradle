apply plugin: "java"
apply plugin: "kotlin"
apply plugin: "kotlin-kapt"

dependencies {
  compile libs.rxJava
  compile libs.kotlin
  compile libs.dagger
  kapt libs.daggerCompiler
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"
